{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma migrate deploy --schema prisma/schema.prisma && node server.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}
